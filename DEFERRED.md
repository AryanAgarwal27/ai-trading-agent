# DEFERRED — code follow-ups not done in the stage that surfaced them

Running ledger of deliberate deferrals: work consciously postponed (with the
reason and a proposed landing point), so nothing surfaced mid-stage is silently
lost. This file tracks **code-structure** follow-ups; stage-specific
contract/threshold deferrals also live in the `SPEC.md` change log (referenced
here, not duplicated).

| ID | Surfaced | Item | Reason deferred | Proposed landing |
|----|----------|------|-----------------|------------------|
| D-1 | Stage 8c | Backport the `registry_writer_fn` + `secrets_provider` injection seams from `live_spawn` to `paper_spawn` (extended by 8d/8e: + `rationale_fn`, `review_fn`, `merge_fn`, `arbitrate_fn`, `gate_audit_writer_fn`, `stop_trading_fn`, `build_snapshot_fn`). `paper_spawn` writes the registry inline (`_connect_app_db` / `_upsert_registry_row`) and loads credentials inside `spawn_paper_container`; `live_spawn` (8c) exposes both as seams — cleaner and DB-/credential-stubbable in tests. | Refactoring the working paper path during Stage 8 is out of scope and carries regression risk; the asymmetry is harmless (both paths work). | A dedicated paper-refactor commit, or fold into Stage 11 hardening. **CLOSED in Stage 11e.** Backported the two SPAWN-relevant seams from `live_spawn` to `paper_spawn`: (1) `secrets_provider` — threaded to `spawn_paper_container`, which now loads paper creds via `secrets.load_paper_credentials(provider)` instead of three raw `_required_env` reads (closing the **paper-exchange-key half of D-18**; the now-dead `_required_env` was removed); (2) `registry_writer_fn` — the registry write moved behind an injectable seam (`_write_paper_registry`, own connection, mirroring `_write_live_registry`), with the port read split into `_read_used_paper_ports` (mirroring live's `_read_used_ports`). Both default to the real implementations, so production behaviour is unchanged. The 8d/8e seams the original entry listed (`rationale_fn`, `review_fn`, `merge_fn`, `arbitrate_fn`, `gate_audit_writer_fn`, `stop_trading_fn`, `build_snapshot_fn`) are for the LIVE evaluate/coordinator path, which paper does NOT have — paper's equivalent (`paper_monitor_fn`, `build_context_fn`) was already seamed — so they do not apply to `paper_spawn`. PRESERVED ASYMMETRY (deliberate, behaviour-identical): paper's spawn-FAILURE path still does NOT archive the registry row (unlike live) — the seam covers only the initial + URL writes paper already performed; mirroring live's failure-archive would be a behaviour change, out of this pure-refactor's scope. PURE REFACTOR — existing paper_spawn / paper_subgraph / parent-graph / stage-8 e2e tests pass UNCHANGED; new tests pin the two seams (provider threaded to spawn; injected registry_writer_fn fully replaces the DB write). Verified: mypy 0 (CI-equivalent Docker, cold cache), unit 368, integration (`integration and not freqtrade`) 126. |
| D-2 | Stage 8d | `ruff format .` drift across ~36 pre-existing files since Stage 4; CI's `ruff format --check .` would fail. 8d's own files are format-clean. Stage 8 commits are unpushed so CI has not seen 8i–8d. **CLOSED in Stage 8 housekeeping: `ruff format .` applied to all 39 drifted files. CI's `ruff format --check .` now passes.** | Mixing 36 files of mechanical formatting into a decision-heavy commit pollutes the audit trail. | A dedicated `chore: ruff format .` housekeeping commit between 8h tag and the Stage 8 push, OR fold into Stage 10's observability/DR hardening pass. |
| D-3 | Stage 8f | `daily_loss_job` uses Freqtrade's **calendar-day** `/api/v1/daily` bucket as an approximation of the BRD §11 "rolling 24h" spec. The daily-loss stopbuy is a SOFT halt (block new entries), not the hard drawdown floor — so the approximation buys the safety property without locking in a semantic argument. | Real rolling-24h math (denominator selection + sliding window) is a semantic decision that gets locked into code and rarely revisited; picking it by guess now is worse than shipping the calendar-day approximation. | Until either (a) an operator-facing scenario demonstrates the calendar-day approximation is materially wrong, or (b) Stage 10 observability surfaces enough data to pick the denominator empirically rather than by guess. |
| D-4 | Stage 8f | `_trailing_losses` is private in `paper.py` and now imported by both `live.py` (8e duplicate copy) and `scheduler.py` (8f import). Three call sites = util candidate. | Refactoring during 8f mixes a util-extraction commit into the kill-switch commit; bad bisect target if anything regresses. | A dedicated `refactor: promote _trailing_losses to orchestrator/util/` commit after Stage 8 ships, or fold into Stage 11 hardening. **CLOSED in Stage 11e.** Promoted to a shared, public `trailing_losses` in `orchestrator/tools/freqtrade_metrics.py` (the `tools/` home fits the existing layout — alongside `freqtrade_api` / `freqtrade_lifecycle` — better than a new top-level `util/`; the helper is a pure metric over Freqtrade `/trades` data). The duplicate copy in `live.py` (the 8e copy) and the original private def in `paper.py` are removed; all three call sites now import the one canonical function — `paper.py` (paper_monitor's consecutive-losses), `live.py` (live reviewer snapshot), `scheduler.py` (kill-switch poll, which previously imported the `_private` name across module boundaries — the smell this closes). PURE REFACTOR: byte-identical logic, behavior unchanged — proven by the existing test suite passing UNMODIFIED (no test imported `_trailing_losses`; it's exercised through paper_monitor / live_evaluate / kill_switch). Verified: mypy 0 (CI-equivalent Docker, cold cache), unit 368, integration (`integration and not freqtrade`) 122. |
| D-5 | Stage 8f | Kill-switch idempotency anchor (`strategy_registry.live_started_at`, Fork 5d) bumps only on `live_spawn`, not on `live_pause`-approve-continue (which doesn't re-spawn). The resume-after-kill suppression hole is **latent**: it is unreachable while `live_pause`-approve→`live_wait` does not `POST /start` (the current 8e topology halts the container and approve does not restart it, so a paused strategy cannot breach the drawdown threshold again). Becomes reachable the moment `/start`-on-resume is wired. **CLOSED in Stage 8g: `/start`-on-resume bumps `live_started_at` via the `start_trading_fn` + `live_started_bump_fn` seams in `live_pause`-approve. The bump is best-effort (a DB-write failure logs loud but won't strand the operator's approve), so a failed bump re-opens the hole for that ONE approve — durable-retry hardening tracked as a Stage-11 item. Verified by `test_kill_guard_unsuppresses_after_live_started_bump`.** **DURABLE-RETRY CLOSED in Stage 11e.** The `live_pause`-approve bump is no longer best-effort: (1) REORDERED — the `live_started_at` bump now runs BEFORE `/start`, so the kill guard is un-suppressed before the container can place a new order; (2) BOUNDED RETRY — `_bump_live_started_with_retry` retries a transient DB failure 3× with exponential backoff (0.2s base), so the common transient blip keeps the hole closed automatically; (3) ULTIMATE-FAILURE = FAIL-SAFE (decision: CLOSE THE HOLE over not-stranding) — if the retries exhaust, the strategy does NOT `/start` (stays HALTED — no live trading without kill-switch protection) and the `kill_switch_event` is NOT cleared, so the next `live_wait` wake routes back to `live_pause` and RE-PROMPTS the operator (who re-approves once the DB recovers); the failure is logged loud AND recorded in the gate decision (`resumed=False, bump_failed=True`) — not silent. JUSTIFICATION: the kill switch is the money-safety floor; resuming live trading with it suppressed risks uncaught capital loss, strictly worse than a safely-halted strategy the operator re-approves — and a persistent DB failure that exhausts retries is itself a reason not to trade (the strategy is halted, not dangerously stranded). Tests (`tests/integration/test_live_subgraph.py`): a transient failure retries-then-succeeds (resumes normally, kill event cleared); an exhausting failure fails safe (no `/start`, kill event persists, gate decision records `bump_failed`, next wake re-prompts `live_pause_review`). Verified: mypy 0 (CI-equivalent Docker, cold cache), unit 368, integration (`integration and not freqtrade`) 124. | Closing the hole now requires bumping `live_started_at` on `live_pause`-approve, which (a) pre-empts 8g's design for the resume path before 8g is designed, and (b) splits the anchor's invariant — `live_started_at` currently means "live container started trading"; bumping on approve drifts the semantics. | When `/start`-on-resume is wired (8g, or a later sub-stage that introduces it): the same code must bump `live_started_at` to `now()`. Owner of that change is responsible for closing this hole and removing D-5. The kill-switch guard query (`fired_at > live_started_at`) requires no change once the anchor bumps correctly. |
| D-6 | Stage 8g | The Redis kill subscription (`orchestrator/kill_subscription.py`, wired in `main.py` lifespan) writes `artifacts.kill_switch_event` into the matching thread's state, but nothing **wakes** a live thread in production to act on it: `schedule_wake` is paper-only (no live-wake job exists), and `/wake` only resumes `kind=="paper_wait"` (it 409s a `live_wait`). So a kill event sets state but the thread does not route to `live_pause` until a live-wake mechanism exists. 8g's tests drive wakes via `Command(resume=...)` directly, so the routing (`_route_after_live_wait` → `live_pause`, kill-switch payload discriminator) is proven; only the production *trigger* is absent. **CLOSED in Stage 9f: production live-wake split implemented.** Kill-path: direct-resume from kill_subscription writer via Command(resume=...) (Option (b), mandated by the 8h finding — parent-aupdate_state clears the parent-visible interrupt, so /wake would 409 a kill-written thread). Periodic re-eval: APScheduler live_wake_<sid> jobs (6h interval, env-overridable, mirrors paper's WAKE_INTERVAL_HOURS) registered at live_spawn + /wake extended for kind="live_wait" + unschedule_live_wake at every live archive site (live_archive, aretire_strategy when stage was "live"). Scheduler owns periodic; supervisor's nightly cron is the backstop for missed wakes. Regression-guarded by test_periodic_wake_refuses_kill_written_thread (unit + integration twin) pinning kill-path/periodic-path independence — the 8h finding's parent-aupdate_state behavior is the contract. | Wiring a live-wake is a real design decision that 8g should not pre-empt: either (a) an APScheduler live-wake job mirroring paper's `schedule_wake` + extending `/wake` to accept a `live_wait` kind, or (b) having the kill subscription itself resume the thread immediately after writing the event (no scheduler round-trip). Picking one before it's designed bakes in the choice. **8h finding (`test_stage_8_e2e`): the subscription's parent-level `graph.aupdate_state` writing the kill event onto a thread parked at the NESTED `live_wait` interrupt CLEARS the parent-visible interrupt surface — `aget_state().tasks[*].interrupts` goes empty though the nested subgraph stays parked and resumes correctly via `Command(resume=...)`. This makes option (a) harder than it reads: the existing `/wake` (and `/approve`) endpoints gate on interrupt-presence and would 409 a kill-written thread, so reusing them requires loosening that guard — option (b) (direct resume from the subscription, bypassing the endpoint guard) sidesteps it.** **Operator-visibility consequence: in the window between the kill write and the wake, `GET /threads` reports `has_pending_interrupt=False` for the thread even though it IS parked, which pushes toward option (b) for a prompt, observable transition.** | A later sub-stage that introduces live-thread wake scheduling (8h or the Stage 9 supervisor). The owner wires either the live-wake job + `/wake` `live_wait` support, OR direct resume from the subscription, and removes D-6. |
| D-7 | Stage 8 CI catch-up | Integration test suite (`tests/integration/`, ~95 tests) has NEVER run in CI. CI's unit job runs `pytest -m "not integration" tests/unit`; no integration job exists. These tests require Postgres + Redis service containers, `alembic upgrade head`, and (for the @freqtrade-marked subset) opt-in real-spawn env vars. Same "deferred-but-unvalidated" class as the mypy drift (Stage 7e → 8 catch-up) and the LANGGRAPH_CHECKPOINT_URI gap (b1bb707). | An integration CI job is a non-trivial workflow addition (services, healthchecks, migration step, env var setup) that's out of scope for a Stage 8 catch-up commit. Adding it now would mix CI infrastructure work into a tag-blocking push. | Stage 9 (Supervisor) or Stage 10 (Observability + DR) — a dedicated `ci: integration job with postgres + redis services` commit. Owner verifies all ~95 integration tests pass against the CI service containers before declaring the gap closed. Until then, integration tests are operator-local-validation-only, same posture as the @freqtrade real-spawn tests. **CLOSED in Stage 9g: integration CI job added.** Workflow `.github/workflows/ci.yml` job "pytest integration (postgres + redis)" runs against pgvector/pgvector:pg15 + redis:7 service containers with healthchecks + `alembic upgrade head` + env stubs for required services. Runs `pytest -m "integration and not freqtrade"` on every push/PR (Python 3.13 only — integration exercises service wiring, not language-version features; the unit matrix covers 3.11-3.13), excluding the @freqtrade real-spawn subset (6 tests) which stays opt-in via AIT_RUN_REAL_SPAWN_TESTS / AIT_RUN_REAL_LIVE_SPAWN_TESTS. Verified **84/84** non-freqtrade integration tests pass against the CI service containers (run locally against an identical pgvector:pg15 + redis:7 stack). The integration suite is now CI-validated; deferring tests with `@pytest.mark.integration` is no longer a silent CI gap. NOTE: the first CI-equivalent run surfaced a 9f regression (test_stage_8_e2e double-fired stop_trading because its 8h manual-resume stacked on 9f's new auto-resume) — fixed in the same commit — demonstrating D-7's value immediately. |
| D-8 | Stage 9c | The supervisor agent is STATELESS-PER-RUN in v1: `build_supervisor_agent` wires no checkpointer, so each `run_supervisor` invocation reasons fresh from the portfolio snapshot / regime / active-strategies passed in its kickoff message, and the durable decision record is the `telemetry` row (`source="supervisor_decision"`), NOT an LLM conversation. The unbounded message-history growth originally feared for a persistent `thread_id="supervisor"` conversation (BRD §5.1) therefore CANNOT occur in v1 — there is no accumulating conversation to grow. `thread_id="supervisor"` is passed in the invoke config as the conceptual thread id only (harmless without a checkpointer). | Dormant-by-design, not accruing debt: this becomes a live concern ONLY if a future sub-stage deliberately reverses the v1 stateless design by wiring the saver into `run_supervisor` for persistent supervisor memory. Turning persistence on without bounded-context management (per-run summarization, or a sliding-window retention policy) would reintroduce linear input-token-cost growth over operating time — but that is a property of THAT future change, not of the v1 stateless runner. | No action while the supervisor stays stateless (the v1 design). IF persistence is ever deliberately added, the owner of that change MUST land bounded-context management in the SAME change (summarization vs. windowing, informed by Stage 10 LangSmith cost curves) and remove D-8. Until then D-8 stays open as a guard-rail on that future decision, not as debt. |
| D-9 | Stage 9b | Transition-time enforcement of `MAX_CONCURRENT_LIVE_STRATEGIES` is ABSENT. The threshold is defined (`thresholds.py`, BRD §10) and the supervisor's spawn gate deliberately does NOT enforce it (spawn-time projection on a cap of 1 would refuse every research spawn while any strategy is live — strangling the pipeline; see the 9b live-cap timing decision). But the live-admission path (`live_spawn` / `live_gate` in the live subgraph) does not yet check `aget_portfolio_snapshot()["live"] >= MAX_CONCURRENT_LIVE_STRATEGIES` before admitting a paper-graduate to live. So with the v1 cap of 1, nothing in code prevents a second concurrent live strategy if two paper-graduates are approved — the operator's HITL approval at `live_gate` is the only de-facto control today. | The enforcement carries an undecided sub-fork: when a paper-graduate is HITL-approved but the live slot is occupied, the gate must (a) reject (archive), (b) queue/hold (park until the slot frees), or (c) block the human gate from offering approve. This interacts with `live_gate` being a human `interrupt()` and is a real design + UX decision (capital-adjacent) that must not be guessed. | A later sub-stage touching the live-admission path (9f live-wake work is adjacent, or Stage 11 hardening). Owner decides the (a)/(b)/(c) sub-fork FIRST (operator sign-off — money-adjacent), then wires the `live` count check into live admission and removes D-9. Until then `MAX_CONCURRENT_LIVE_STRATEGIES` is defined-but-unenforced; HITL at `live_gate` is the control. **CLOSED in Stage 11d — operator chose Option (c) BLOCK THE GATE.** `live_gate` (`orchestrator/subgraphs/paper.py`) now reads `aget_portfolio_snapshot()["live"]` (via an injected `LiveCountFn` seam, default `_default_live_count`) BEFORE presenting the approve option: if `live >= MAX_CONCURRENT_LIVE_STRATEGIES`, it does NOT `interrupt` for approval and does NOT promote — it records a `"live_slot_occupied"` status in `gate_decisions["live_gate"]` and routes back to `paper_wait`. The existing 6h paper wake then re-runs `paper_monitor → divergence_check → live_gate` each wake; once the operator frees the slot (pauses/stops the live strategy), a later wake reaches `live_gate` with a free slot and offers approval normally. NO auto-promotion — the human still approves every live entry (BRD §17 #8). NOT (a) reject/archive, NOT (b) auto-queue. Re-park is on `paper_wait` (not a new interrupt kind) deliberately: `/wake` 409s on kind-mismatch and the paper wake job fires `kind="paper_wait"`, so a custom kind would be unreachable. Surfaced for the operator: `GET /threads` gains `live_gate_status` (`"live_slot_occupied"`) and the Streamlit dashboard renders `waiting · live slot occupied`. Tests (`tests/integration/test_paper_subgraph.py`): slot-full → parks at `paper_wait`, never offers the gate, stage stays `paper`, no `"live"` approval across multiple wakes (no auto-promotion); slot frees `[1,0]` → next wake offers approval, human-approve is the ONLY promotion path. Verified: mypy 0 (CI-equivalent Docker, cold cache), unit 367, integration (`integration and not freqtrade`) 120. |
| D-10 | Stage 9b | `sync_registry_stage` reconciles `strategy_registry.stage` only — it does NOT sync `template`. `aspawn_strategy` seeds the registry row with `template="pending"` at research-spawn; the researcher chooses the real template later in graph state, so the registry `template` stays `"pending"` for the strategy's whole lifecycle. The dashboard / audit view shows `"pending"` instead of the chosen template. **9h decision: DEFERRED to Stage 10 (not closed in 9h).** v1 ships with template-drift acceptable because the supervisor's reasoning ground-truth IS the registry (it never reads graph state), and `template` is a benign label in `list_strategies` — the agent spawns/retires, it does NOT branch on `template`, so `"pending"` is a cosmetic dashboard/audit blemish, not a routing hazard. Closing ad-hoc in 9h would also force the overwrite-vs-skip contract question (how should sync treat a field that is a seed value in the registry but truthy in graph state?) one field at a time; that contract belongs declared ONCE for all mirrored fields in Stage 10's reconciliation work (BRD §16 `reconcile.py`), not piecemeal. **CLOSED in Stage 10f (registry↔graph-state mirror contract declared in ONE place — `orchestrator/supervisor.py` `MirrorField` / `MIRRORED_FIELDS` / `_mirror_value` — and applied generically in `sync_registry_stage`; see the Stage 10f COMMIT 2 hash in `git log --grep "Stage 10f.*D-10"`). DECLARED CONTRACT: (1) WHICH fields mirror graph state — `stage` + `template` are IN (the GRAPH is their source of truth: stage is the lifecycle position, template is seeded `"pending"` and chosen by the researcher in graph state); `pairs` / `timeframe` / `name` are OUT (spawn INPUTS — the registry is their source of truth; no graph node writes them back, so mirroring would be a no-op at best and could clobber the operator's spawn record). (2) OVERWRITE-VS-SKIP RULE (the open question): graph state WINS for a mirrored field once it holds a REAL value — overwrite iff the graph value is truthy, NOT a per-field seed sentinel (`template`'s is `"pending"`; `stage` has none), and differs from the registry value; otherwise SKIP (never overwrite a real registry value with an empty/seed graph value). Extended generically so future mirror fields follow the same rule (add a `MirrorField` row, not a code branch). The registry `template` is now reconciled off `"pending"` on the next supervisor tick once the researcher chooses; a dedicated per-transition timestamp stays OUT of the mirror set (Stage 11 candidate — it is registry-write metadata, not graph-derived).** | Extending the sync to also reconcile `template` (and possibly `pairs`/`timeframe`) from graph state is straightforward, but out of 9b's write-tools scope; bundling it into the capacity commit would mix concerns. Picking WHICH identity fields the registry should mirror is a small design choice better made alongside the 9c runner that drives the sync. | Stage 10 reconciliation (BRD §16 `reconcile.py`) — declare the registry↔graph-state mirror contract for ALL derived fields (`template`, and any future drifters like a per-transition timestamp) in one place rather than piecemeal-extending `sync_registry_stage` per field. Owner extends the sync per that contract and removes D-10. (9h considered closing it here but deferred — see the Item column.) **DONE — Stage 10f.** |
| D-11 | Stage 9e | Supervisor concurrent-run race on the capacity gate. The 9d nightly cron (`id="supervisor_cron"`) and the 9e event-driven one-shot (`id="supervisor_event_oneshot"`) are DISTINCT APScheduler job ids, so `max_instances=1` (which is per-job-id) does NOT serialize them against each other. If a debounced event run's fire time lands during the ~03:15 UTC cron run, two `run_supervisor` invocations execute concurrently on separate connections; each does `sync_registry_stage → snapshot → reason`, and `aspawn_strategy`'s capacity gate is read-then-write WITHOUT a row lock — so a rare cross-run interleave could momentarily admit one spawn over `MAX_CONCURRENT_STRATEGIES` (active overshoot by 1). ~0.06%/day overlap probability (≈55s vulnerable window per 24h cycle, given the cron's ~15-25s run time + the 30s event debounce). Bounded consequence: a momentary `active` overshoot of 1 that self-corrects on the next sync; NO money loss (the live cap is enforced separately at transition-time per D-9, not on this resource axis) and NO state corruption. | The proper fix is a serialization primitive (a Postgres advisory lock around `run_supervisor`) — non-trivial: it needs a timeout, a holder id, and crash-cleanup semantics. That is the SAME shape as D-9's capacity-gate serialization problem, so picking and wiring ONE lock mechanism for both is cleaner than two ad-hoc fixes. `max_instances=1` was shipped per the 9e spec (it correctly prevents same-job-id overlap — back-to-back event runs); cross-job-id overlap is the residual. | Stage 11 hardening, fixed TOGETHER with D-9 (one advisory-lock mechanism serializing every `run_supervisor` invocation — cron, event, and manual — and closing the capacity-gate read-then-write window on both the resource axis (D-11) and the live/capital axis (D-9)). Owner adds the lock and removes D-11. Until then the overlap is a documented, bounded, self-correcting v1 limitation. **CLOSED in Stage 11d.** ONE serialization primitive now wraps `run_supervisor` (`orchestrator/supervisor.py`): `_acquire_supervisor_lock` issues `SELECT pg_advisory_xact_lock(_SUPERVISOR_ADVISORY_LOCK_KEY)` as the FIRST statement inside the run's transaction (before `sync_registry_stage`), so cron, event, AND manual (`/supervisor/run`) invocations serialize on the same fixed key — a concurrent run BLOCKS on acquire and reads capacity only AFTER the holder commits, closing the read-then-write window. CHOICES (per the brief): TRANSACTION-scoped (`pg_advisory_xact_lock`) over SESSION-scoped (`pg_advisory_lock`) because the xact lock auto-releases when the run's transaction ends (commit / rollback / connection close) — crash-safe, no leaked lock stranding the next run; and a BLOCKING wait over `pg_try_*`/skip because waiting serializes without DROPPING the second run's work (it still reasons, on fresh post-commit state). Tests: unit (`tests/unit/test_supervisor_runner.py`) asserts the lock is the first SQL and precedes every write; integration (`tests/integration/test_supervisor_lock.py`) proves exclusivity + auto-release-on-conn-close and that two concurrent `run_supervisor` serialize (the second blocks until the holder releases, then completes). This closes BOTH the resource axis (D-11) and is wired alongside D-9's capital-axis gate. Verified: mypy 0 (CI-equivalent Docker, cold cache), unit 367, integration 120. |
| D-12 | Stage 9f | Paper-side wake-job cleanup gap. `paper_teardown` (paper.py archive node) stops the container, writes registry, and publishes the thread_completed event, but does NOT call `unschedule_wake(strategy_id)` on the persisted `wake_<strategy_id>` SQLAlchemy job. Consequence: an archived paper thread's 6h wake continues firing forever, each invocation hitting `/wake` and getting a 409 (no interrupt, no resume). Same shape as the live-side cleanup gap that 9f closed by adding `unschedule_live_wake` at every live archive site — but pre-existing on paper and out of 9f scope. | Adding `unschedule_wake(strategy_id)` to `paper_teardown` (and to any other paper archive path that bypasses paper_teardown — paper_spawn-failures route to the archive sink without going through teardown, so they also leak the same way) is mechanical, but it's symmetric scope to 9f and deserves its own commit + test for clarity. | Stage 11 hardening — fold alongside the symmetric paper cleanup pass. Owner mirrors 9f's pattern: `unschedule_wake` calls at every paper archive site (paper_teardown + paper_spawn failure paths) + a test asserting the call. Until then: orphan paper wake jobs fire every 6h and harmlessly 409 — same posture as 9f's "crash-between-archive-and-cleanup orphan" residual. **CLOSED in Stage 11e.** Mirrors 9f's terminal-node pattern: the paper `archive` node (`orchestrator/subgraphs/paper.py`) — the SINGLE terminal sink for BOTH archive routes (the kill/reject path `divergence_check`/`live_gate` → `paper_teardown` → `archive`, AND the spawn-failure path `paper_spawn`→archived → `_route_after_spawn` → `archive`, bypassing teardown) — now calls an injected `unschedule_wake_fn(strategy_id)`, best-effort (a cleanup failure logs but never strands the archive, exactly like `live_archive`). Putting it at the common terminal covers every archive flow in ONE place. Sites found: (1) `paper_teardown` → `archive` (kill via divergence_check, reject via live_gate); (2) `paper_spawn`-failure → `archive` (no wake was ever scheduled there — the cancel is an idempotent no-op, but the call site fires so the contract holds at every site). Production wiring: `scheduler.make_unschedule_wake_fn` removes `wake:strategy_<sid>` (the paper wake job is keyed on thread_id == `strategy_<sid>` by the spawn convention; idempotent on a missing job), threaded `main → build_per_strategy_graph(unschedule_wake_fn=…) → build_paper_subgraph`. Tests: integration (`tests/integration/test_paper_subgraph.py`) asserts the cancel fires at BOTH archive sites (kill/teardown + spawn-failure); unit (`tests/unit/test_scheduler.py`) pins the `wake:strategy_<sid>` job-id reconstruction + idempotent no-op on a missing job. Verified: mypy 0 (CI-equivalent Docker, cold cache), unit 368, integration (`integration and not freqtrade`) 122. |
| D-13 | Stage 10c | Windows full-suite test-ordering flakiness in `tests/unit/test_live_wake.py`: `test_kill_writer_direct_resumes_after_state_write` and `test_kill_writer_resume_failure_is_swallowed` FAIL under a full `pytest -m "not integration"` run (`ValueError: The future belongs to a different loop than the one specified as the loop argument`, raised inside the kill_subscription direct-resume path), but PASS when `test_live_wake.py` is run in isolation. Verified pre-existing and independent of Stage 10c: the SAME 2 tests fail identically at clean HEAD (`git stash -u`, no 10c files present) and reproduce with the new `test_logging.py` excluded — so this is NOT introduced by the structlog substrate. Cause is cross-test event-loop contamination on Windows (a per-test loop from an earlier async test outliving its scope, then a coroutine/future from `test_live_wake` being awaited against a different loop), the **second** occurrence of the py-async event-loop quirk family on this repo (cf. SPEC 2026-05-27 Stage 3c, where psycopg-async-needs-`SelectorEventLoop` vs native-async-subprocess-needs-`ProactorEventLoop` forced the `asyncio.to_thread(subprocess.run)` workaround in `backtest_runner.py`). | Windows-only test-harness artifact, not a product bug — the kill direct-resume code path itself is correct (proven green in isolation and on Linux CI). Chasing the precise loop-leak across the unit suite's test ordering is a test-isolation investigation (likely an `event_loop`/`event_loop_policy` fixture-scope fix or per-test loop teardown), disproportionate to fold into the Stage 10c wiring commit and orthogonal to the structlog work. | Stage 11 test-isolation pass — pin per-test event-loop teardown (or hoist the offending async fixtures to a consistent scope) so the full Windows unit suite is order-independent, then add a guard that the full-suite and isolated runs of `test_live_wake.py` agree. Until then: Linux CI is the gate (green there), and the 2 failures are a documented Windows-local full-suite artifact. Owner removes D-13. **CLOSED in Stage 11a — root cause SUBSUMED by the D-15 fix.** The cross-loop `_KILL_RESUME_TASKS` leak that made `test_live_wake`'s 2 kill tests collection-order-dependent is neutralised suite-wide by conftest's `_isolate_module_task_registries` autouse fixture (both task registries cleared on their owning loop after EVERY test) + the import-time Selector policy. The 2 kill tests now pass under every collection order exercised (default, reverse-file, loop-sensitive-first); the per-file "documented + Linux-CI-gated" workaround is superseded by the structural guarantee, and `tests/unit/test_loop_isolation.py` guards against regression. See the D-15 row for the full mechanism + proof. |
| D-14 | Stage 10d closure | **RESOLVED in this commit — recorded for the pattern.** Windows production launch was BROKEN: the orchestrator had no production entry point that installs a `SelectorEventLoop`, so on win32 a plain `uvicorn.run()` / `uvicorn --loop asyncio` forces uvicorn's `ProactorEventLoop`, under which the lifespan's `AsyncPostgresSaver.from_conn_string` crashes (psycopg async requires Selector). The app had only ever run under pytest, where `tests/conftest.py` installs `WindowsSelectorEventLoopPolicy` as a fixture (why the 87 integration tests pass); `docker-compose.yml` is postgres+redis only (the app runs on the host). BRD §3 explicitly supports a local machine, so Windows-native launch is a SUPPORTED config. This is the **third** occurrence of the py-async event-loop quirk family on this repo: (1) SPEC 2026-05-27 Stage 3c — psycopg-Selector vs subprocess-Proactor, forcing `asyncio.to_thread(subprocess.run)` in `backtest_runner.py`; (2) D-13 — Windows full-suite test-ordering loop contamination; (3) this — production launch. | Not deferred — FIXED here: `orchestrator.main.main()` installs `WindowsSelectorEventLoopPolicy` on win32, then serves a `uvicorn.Server` via `asyncio.run` (owning the loop, since uvicorn would otherwise replace a policy-set loop). Operator-confirmed mechanism. Linux is unaffected (Selector is the default there; the win32 branch is skipped). Severity of the bug = **local Windows launch only**; the Linux VPS prod target was never affected. | Closed by this commit (`def main()` + `__main__` in `orchestrator/main.py`, launch command documented in `README.md`, unit test pinning the per-platform loop-policy install). No further work; entry kept as the durable record of the third event-loop incident so a future fourth is recognised as a family, not a one-off. **Stage 11a follow-up (via D-15):** the test-side local patch — stubbing `asyncio.run` in `test_main_entrypoint` so it never touched a real loop — is REMOVED. `main()`'s policy install is extracted to `_install_selector_loop_policy_on_win32`, and the test now exercises that helper directly (no loop, no stub) — a structural replacement for the stub. The PRODUCT launch fix (this row) is unchanged. |
| D-15 | Stage 10d closure | **Systemic root cause behind D-13 + the D-14 commit's test-pollution: no enforced per-test event-loop isolation.** This is a MEASUREMENT-INTEGRITY risk, distinct from the per-file patches already applied. Two instances now: (1) D-13 — `test_live_wake.py` fails only under a particular full-suite collection order; (2) the D-14 commit's `test_main_entrypoint.py` — its first version called the real `asyncio.run` inside `main()`, which created+CLOSED an event loop and broke an UNRELATED later test (`test_risk_analyst`) — green in isolation, red in-suite, purely as a function of pytest collection order. Root cause both share: a test that touches a real event loop can leave the loop closed / detached for a later test, and nothing in the harness guarantees each test gets a fresh, isolated loop. Consequence: **a green unit-suite run is only green for THAT collection order** — reorder it (a new test file, `pytest-randomly`, a different shard/-k filter) and a latent failure can surface, or a real regression can hide. Each instance was patched LOCALLY (D-13: documented + Linux-CI-gated; D-14 test: stubbed `asyncio.run` so the test never touches a real loop), but neither installs a suite-wide guarantee. **CLOSED in Stage 11a.** Root cause reproduced DETERMINISTICALLY (a throwaway probe: test A leaks an un-drained task into `kill_subscription._KILL_RESUME_TASKS`, test B gathers the set on its own loop → `RuntimeError: ... Future ... attached to a different loop`; swapping the two tests' order flips pass↔fail). The mechanism both D-13 and the D-14 test share: the module-level fire-and-forget task registries (`kill_subscription._KILL_RESUME_TASKS`, `supervisor._BACKGROUND_SPAWN_TASKS`) OUTLIVE pytest-asyncio's per-test function-scoped loop, so a task created on test A's (now-closed) loop and left in a registry is awaited by a later test B on B's loop — and pytest COLLECTION ORDER decides whether A precedes B. FIX (`tests/conftest.py`): (1) install `WindowsSelectorEventLoopPolicy` at conftest IMPORT time on win32 — bulletproof, not merely via the session `event_loop_policy` fixture — so even a stray loop (a rogue `asyncio.run`, or sync `get_event_loop`) is a Selector loop; (2) an async autouse function-scoped fixture `_isolate_module_task_registries` whose teardown runs on the test's OWN loop (verified for sync AND async tests) and cancels+drains+clears BOTH registries after every test, so no task can cross into the next test's loop. PLUS the D-14 entrypoint test is made STRUCTURAL (not stubbed): `orchestrator.main._install_selector_loop_policy_on_win32` is extracted so `test_main_entrypoint` asserts the per-platform policy install with NO real loop and NO `asyncio.run` stub — removing the D-14 local patch entirely. PROVEN order-independent: the full `-m "not integration"` unit suite is IDENTICAL (362 passed) across 3 collection orders — default (alphabetical), reverse-file, and loop-sensitive-files-first — and a PERMANENT regression guard `tests/unit/test_loop_isolation.py` leaks a task into both registries then asserts the next test sees them empty (fails loudly if the fixture is ever removed). D-13 root cause is SUBSUMED (the `test_live_wake` kill tests can no longer flake — the registry they gather is always clean at test start); D-14's PRODUCT launch fix stays, only its test-side stub is replaced. Verified: mypy **0/116** via the D-17 CI-equivalent (Docker `python:3.12-slim` + `uv sync --locked` + `uv run mypy orchestrator tests`), unit **362**, integration (`integration and not freqtrade`) **101**. | A proper fix is a cross-suite test-harness change — a function-scoped event-loop fixture with deterministic teardown (or pinning pytest-asyncio's loop scope/policy suite-wide), PLUS a lint/guard that forbids a real `asyncio.run` / loop creation inside unit tests — a focused test-infra pass that interacts with the existing `event_loop_policy` fixture (conftest.py) and pytest-asyncio auto mode. Disproportionate to fold into a feature/bugfix commit, and it must not silently change the 87-integration / 347-unit baselines while doing so. | Stage 11 test-hardening pass — install enforced per-test loop isolation, then PROVE order-independence (run the unit suite under a shuffled collection order in CI — e.g. `pytest-randomly` — and require it green) so the gate stops being collection-order-dependent. This closes D-13 and removes D-15; the D-14 launch fix stays (it is product code, not test infra). Until then: keep stubbing real loops in unit tests, and trust Linux CI (fixed seed/order) as the gate. **DONE — Stage 11a.** |
| D-16 | Stage 10d closure | **Supervisor spawn vocabulary is unconstrained — it can propose templates/pairs that do not exist.** Surfaced BY the 10d trace-verification run: in the real LangSmith-captured supervisor ReAct loop, the agent proposed `SupervisorAction`s naming templates `stat_arb` and `momentum` and the pair `AVAX/USDT` — NONE of which ship. The v1 templates are `mean_reversion` / `freqai_classifier` / `freqai_regressor` (BRD §8.1) and the v1 pair universe is BTC/ETH/SOL/BNB-USDT (SPEC §1 Q2). Neither the supervisor's tools nor its prompt (`_SUPERVISOR_PROMPT`) constrain the spawn vocabulary — `SupervisorAction.template` / `.pairs` are free-form `str` / `list[str]` (the researcher is *supposed* to pick the template, so spawn-time template is optional and defaults to `"pending"`, but a hallucinated non-empty value would still be seeded into the registry row). HARMLESS in this instance because the verification used `dry_run` (no writes), but on a REAL `/supervisor/run` (or a cron/event run) a hallucinated `template`/`pairs` would seed an unfillable registry row (a template the generator can't load) or a dataless pair (no cached OHLCV, no Binance whitelist entry) — a strategy that wastes a capacity slot and fails downstream. **CLOSED in Stage 11b — defense-in-depth, both layers.** (1) VALIDATION (the load-bearing guarantee): `aspawn_strategy` now rejects a spawn AT THE BOUNDARY — before the capacity read and before any registry write — when `template ∉ generator.SHIPPED_TEMPLATES` (→ `{"spawned": False, "reason": "unknown_template", "template", "allowed_templates"}`) or any pair `∉ supervisor._PAIR_UNIVERSE` (→ `reason="pairs_outside_universe"`, `invalid_pairs`, `allowed_pairs`); no row inserted, `spawn_thread_fn` not called — the SAME no-write contract as the `capacity_exceeded` refusal. DECISION = REJECT the whole action, NOT clamp: a hallucinated template means the agent misunderstood its option set; silently dropping the template to `pending` or filtering bad pairs would seed a registry row that contradicts the action's own `rationale` (the operator audit would then misrepresent what was decided) and spend a capacity slot on something the agent never intended — louder + safer to refuse and let the next tick re-decide against the corrected prompt. `template=None` / `pairs=None` (the normal "researcher chooses / default universe" case) pass the gate. SINGLE SOURCE OF TRUTH per set, no duplicated lists: templates = `generator.SHIPPED_TEMPLATES` (a `frozenset` DERIVED from `_SCHEMA_CLASS_NAMES`, the very map `load_schema` uses to render — a template not there already raises `KeyError` at generation, so the set IS the renderable-template registry); pairs = `supervisor._PAIR_UNIVERSE` (a `frozenset` of `_DEFAULT_PAIRS`, the existing SPEC §1 Q2 constant). (2) PROMPT (reduces how often (1) fires): `_SUPERVISOR_PROMPT` now enumerates the exact valid templates + pairs, INTERPOLATED from those same two canonical constants, so the prompt can never drift from the validator. Tests (`tests/unit/test_supervisor_capacity.py`): bad template rejected no-write, bad pair rejected no-write (only the offender flagged), fully-valid action still spawns, omitted template/pairs passes the gate — all asserting the registry no-write on rejection, mirroring the capacity-refusal pattern. Verified: mypy **0/116** (CI-equivalent Docker, cold cache, exact CI command `uv run mypy orchestrator tests`), unit **366**, integration **101**. | A proper fix is input-validation on the supervisor's emitted decision — reject/clamp `SupervisorAction.template ∉ {shipped templates}` and `SupervisorAction.pairs ⊄ SPEC §1 Q2 universe` BEFORE `aspawn_strategy` seeds the registry — plus tightening `_SUPERVISOR_PROMPT` to enumerate the real template + pair vocabulary. It is a real validation-layer + prompt change (where to enforce: pydantic validator on `SupervisorAction`, or a guard in `_execute_action`/`aspawn_strategy`; whether to hard-reject vs clamp-to-default), money-adjacent-ish (a bad spawn burns a capacity slot), and out of scope for the 10d trace-verification closure. | Stage 11 hardening — validate `SupervisorAction.template ∈ shipped templates` (mean_reversion / freqai_classifier / freqai_regressor) and `.pairs ⊆ SPEC §1 Q2` (BTC/ETH/SOL/BNB-USDT) at the spawn boundary, and enumerate the allowed vocabulary in `_SUPERVISOR_PROMPT`. Owner decides reject-vs-clamp. Until then: the unconstrained vocabulary is a documented v1 limitation, latent behind HITL + the researcher's own template choice; `dry_run` runs are unaffected. **DONE — Stage 11b** (reject-not-clamp; gate in `aspawn_strategy`). |
| D-17 | Stage 10d (CI mypy fix) | **Local Windows mypy can report a FALSE 0 that Linux CI catches — measurement-integrity gap; the only trustworthy mypy signal is CI / a Linux `uv run mypy`.** SECOND local↔CI mypy divergence on this repo (the first was the entire 10a baseline, SPEC change-log 2026-05-28 → "closed" by the uv lockfile). The 10a fix closed **library-version** drift (lock pins identical dep versions local+CI); it did NOT — and cannot — close THIS divergence, which is a **platform / mypy-binary** difference, not a dependency-version one. Concretely (Stage 10d push): `tests/unit/test_logging_propagation.py:79`'s `add_node(closure)` produced error code **`call-overload` on Windows** but **`arg-type` on Linux** (different resolved overload path for the same langgraph stubs), so a code-specific `# type: ignore[call-overload]` was simultaneously *used* on Windows and *unused + non-covering* on Linux — Windows `uv run mypy --python-executable <venv>` reported **0** while CI (Linux `uv run mypy`) reported **2 errors** (`unused-ignore` + `arg-type`). Two Windows-local invocations are BOTH unreliable: the compiled `mypy.exe` (`uv run mypy`) mis-derives site-packages once a real import exists (the footgun flagged through 10c/10d, worked around with `--python-executable`), AND even the `--python-executable` workaround resolves platform-divergent error codes. The 10d.2-push fix was verified by reproducing the Linux result in Docker (`python:3.12-slim` + `uv sync --locked` + `uv run mypy orchestrator tests` → 0/107) — that reproduction, not any Windows run, is what makes the fix trustworthy. | A durable fix is to make a Linux-equivalent mypy runnable locally as the pre-push gate — a thin wrapper (Docker `python:3.12-slim` running `uv sync --locked && uv run mypy orchestrator tests`, or a WSL invocation) so contributors get the CI answer before pushing — OR to formally document local mypy as **advisory-only** with CI as the sole gate. It is test-infra/tooling, out of scope for a feature/bugfix commit, and adjacent to D-15 (the other "green-here-isn't-green-on-CI" measurement-integrity gap). | Stage 11 hardening — add a `scripts/mypy-ci.sh` (or Makefile target) that runs the CI-equivalent Linux mypy via Docker/WSL and make it the documented pre-push check, OR document in README/CONTRIBUTING that local mypy is advisory and CI is authoritative. Until then: **never claim mypy 0 from a Windows-only run** — reproduce on Linux (Docker `uv run mypy`) or mark PROVISIONAL pending CI. Owner removes D-17. |
| D-18 | Stage 11c audit | **Not every secret read routes through `orchestrator/security/secrets.py` (BRD §15 item 5, "via secrets.py" half).** Surfaced by the Stage 11c security audit. Reads that bypass the boundary: `OPERATOR_TOKEN` (`main.py:343`, `scheduler.py:149`), `DATABASE_URL` (`observability/events.py:109`, `scheduler.py:201/237/545`, `subgraphs/live.py:180`, `subgraphs/paper.py:108`), the Freqtrade REST passwords `PAPER_API_PASSWORD` / `BINANCE_LIVE_API_PASSWORD` (`observability/freqtrade_exporter.py:73-74`, `subgraphs/paper.py:495`, `subgraphs/live.py:695/787/808`, `scheduler.py:307`, `tools/freqtrade_lifecycle.py:514/784`), and the **paper** exchange keys (`load_paper_credentials` has ZERO callers — the paper path relies on Freqtrade's own `${VAR}` env substitution, the D-1 paper-inline asymmetry). All read raw `os.environ`. **NOT exploitable**: every value comes from the environment (nothing hardcoded or committed — `git grep` for secret literals in tracked files is clean; the paper/live base configs use `${VAR}` placeholders), and the v1 `EnvSecretProvider` IS `os.environ.get`, so routing through it would be byte-identical behaviour today. The money-critical half — `load_live_credentials` with `SecretCollisionError` enforcing paper≠live keys (BRD §1.1 rule 5) — DOES go through secrets.py and is regression-pinned in `tests/integration/test_security_smoke.py`. | Centralizing every secret read earns nothing functional while the only provider is env-based; `secrets.py`'s indirection pays off ONLY when a real backend (sops / 1Password) is adopted — that is the point at which all reads must funnel through it so the backend can intercept them. Routing ~10 call sites now is churn with zero security gain, and the paper-key slice overlaps D-1's paper-inline refactor. | Fold into **D-1**'s `secrets`/seam refactor, OR into the commit that wires a real `SecretProvider` backend (the `SopsSecretProvider` / `OnePasswordSecretProvider` seams already declared in `secrets.py`). That owner routes `OPERATOR_TOKEN` / `DATABASE_URL` / the REST passwords / the paper keys through `secrets.py` and removes D-18. Until then: §15 item 5's security-critical half (no committed/hardcoded secrets) is VERIFIED-OK; the "via secrets.py" half is partial (live exchange creds only). **PARTIALLY CLOSED in Stage 11e (paper exchange keys).** The D-1 paper_spawn refactor added a `secrets_provider` seam routing paper creds through `secrets.load_paper_credentials` — so the **paper exchange keys** now go through secrets.py (and `load_paper_credentials` finally has a caller). REMAINING OPEN (D-18 stays open, re-scoped to these): `OPERATOR_TOKEN` (the FastAPI auth dependency + the scheduler wake-job header), `DATABASE_URL` (every `_connect_app_db` / libpq DSN), and the Freqtrade REST passwords read in the monitoring / exporter / scheduler paths (NOT the spawn path). These are not exploitable today (all env-sourced, nothing committed; `EnvSecretProvider` == `os.environ.get`) and are out of the spawn refactor's scope; they remain best closed when a real sops/1Password backend is wired (the original landing) — at which point auth/db/REST-pw reads funnel through `secrets.py` and D-18 closes fully. |
| D-19 | Stage 11c audit | **No separate DB user per logical DB (BRD §15 item 6, second half).** `db/init/01_create_databases.sql` creates three logical DBs (`app`, `langgraph_checkpoints`, `langgraph_store`) but all are accessed by the single `trading_agent` role (`POSTGRES_USER`). A least-privilege gap: one compromised credential reaches all three DBs. The **SCRAM-SHA-256** half of item 6 IS enforced (`docker-compose.yml:24-25`, regression-pinned in `test_security_smoke.py::test_postgres_uses_scram_auth`); only the per-DB-user separation is missing. | Low risk in v1: single operator, single host, all three DBs in one trust domain, every port bound to `127.0.0.1`, and WireGuard/SSH is the real network boundary (§15 item 3). Per-DB roles require new least-privilege `GRANT`s plus three DISTINCT connection strings (`DATABASE_URL`, `LANGGRAPH_CHECKPOINT_URI`, `LANGGRAPH_STORE_URI`) each with a different user — a non-trivial infra change touching the init SQL + env wiring + the connection helpers, out of scope for an audit commit. | A dedicated infra commit: per-DB roles + least-privilege `GRANT`s in `01_create_databases.sql`, plus per-DB connection strings (separate user per URI) in `.env.example` and the `_connect_*`/`_libpq_dsn` helpers. Owner removes D-19. Until then: single `trading_agent` user, mitigated by loopback-only binds + WireGuard. |

## Related deferrals tracked elsewhere

- **events.py mypy noise** (redis-untyped) → Stage 10 lockfile work item. See `SPEC.md` change log (2026-05-28).
- **BRD §5.7 / §5.8 `freqtrade_process_id` doc update** (int → str rename) → Stage 9 docs pass. See `SPEC.md` change log (2026-05-27).
