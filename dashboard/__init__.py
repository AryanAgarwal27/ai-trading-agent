"""Operator dashboard — Streamlit UI for the HITL gates.

Stage 6f scope: threads list + paper_gate card.
Stage 7+: live_gate card (Haiku monitor rationale).
Stage 8+: live_pause_review card (coordinator + kill-switch paths).

Launch with::

    .venv/Scripts/streamlit run dashboard/app.py --server.address 127.0.0.1
"""
